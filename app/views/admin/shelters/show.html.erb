<%= @shelter.name %>
<%= "#{@shelter.street} #{@shelter.city} #{@shelter.zip}" %>
<%= "Statistics" %>
<%= "Average pet age: #{@shelter.adoptable_pets.average_age}" %>
<%= "Number of adoptable pets: #{@shelter.adoptable_pets.num_pets}" %>